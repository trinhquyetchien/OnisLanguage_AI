{
  "name": "onislanguage-ai-root",
  "version": "1.0.0",
  "description": "Unified AI platform for Japanese Learning",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && python -m app.main",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
